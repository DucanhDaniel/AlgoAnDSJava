package JustSomeAlgo;
public class Entry {
    public static void main(String[] args) {
        // Scanner sc = ScannerUtils.getScanner();
        BFSDFSTest.test(null);
        DijkstraTest.test(null);
        
        ScannerUtils.closeScanner();
    }
}
